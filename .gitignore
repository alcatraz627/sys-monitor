# SwiftPM
.build/
.swiftpm/
Package.pins
*.xcodeproj/
*.xcworkspace/
DerivedData/
xcuserdata/
*.xcuserstate
*.xcscmblueprint

# Built .app bundles (build.sh assembles them; not tracked)
*.app/

# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# Claude Code agent artifacts (entire dir — internal tooling, not project source)
.claude/
_*.claude.md
**/.playwright-mcp/

# Scratch / personal
*.local
.env
